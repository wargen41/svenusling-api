    <style>
        input:focus,
        select:focus,
        textarea:focus
        {
            outline-color: var(--text-color-splash-faint);
        }

        main > details
        {
            padding-top: .5rem;
            padding-bottom: 1rem;
            padding-inline: 1rem;
        }

        main > details > summary
        {
            margin-top: -.5rem;
            margin-right: -1rem;
            margin-bottom: .5em;
            margin-left: -1.7em;
        }

        details > summary
        {
            margin-left: -1em;
            margin-bottom: .8em;
        }

        details > details + details
        {
            margin-top: 1em;
        }

        details > details > * + *
        {
            margin-block: .5em;
        }


        fieldset input[type=text],
        fieldset input[type=number],
        fieldset input[type=date]
        {
            /*border-radius: 10em;*/ /* rounded corners "pill shape" */
        }

    </style>
