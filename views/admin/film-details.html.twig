{% extends "admin/base.html.twig" %}

{% block mainContent %}
{% if item == null %}
        <pre>ERROR item is null</pre>
{% else %}

    <article>
        <h1>{{ item.title }}</h1>

{%include "admin/forms/details-film-core.html.twig" %}
    </article>

{% endif %}
{% endblock %}
