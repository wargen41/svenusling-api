    <script>
        const ratingSelectors = document.querySelectorAll('.rating-selector');

        ratingSelectors.forEach(selector => {
            selector.addEventListener('input', function(event) {
                // Get the number input and range input
                const numInput = selector.querySelector('input[type=number]');
                const rangeInput = selector.querySelector('input[type=range]');

                // Update the other input with its value
                if (event.target === numInput) {
                    rangeInput.value = numInput.value;
                } else if (event.target === rangeInput) {
                    numInput.value = rangeInput.value;
                }
            });
        });
    </script>
